# Eployee_retention_proj
This project focuses on predicting employee retention using machine learning models such as LightGBM and XGBoost. The project also includes an API layer built using FastAPI to serve predictions.
